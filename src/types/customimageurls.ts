/**
 * Custom ImageUrl configuration object.
 * 
 * This interface contains the attributes and subitems that represent a
 * custom image url configuration item.
 */
export interface CustomImageUrls {
  [title: string]: string;
}
